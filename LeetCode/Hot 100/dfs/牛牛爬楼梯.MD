    public void dfs(int sumStep, int last, int llast, int n, int m) {
        if (sumStep > n) {
            return;
        }
        if (sumStep == n) {
            res = (res + 1) % 10000007;
            return;
        }
        for (int i = 1; i <= m; i++) {
            if (i == last || i == llast) {
                continue;
            }
            dfs(sumStep + i, i, last, n, m);
        }
    }

    Map<String,Integer> map = new HashMap<>();
    public int dfs2(int sumStep, int last, int llast, int n, int m) {
        if (sumStep > n) {
            return 0;
        }
        if (sumStep == n) {
            return 1;
        }
        String str = sumStep+","+last+","+llast;
        if(map.containsKey(str)){
            return map.get(str);
        }
        int res2 = 0;
        for (int i = 1; i <= m; i++) {
            if (i == last || i == llast) {
                continue;
            }
            res2+=dfs2(sumStep + i, i, last, n, m);
        }
        res2=res2%10000007;
        map.put(str,res2);
        return res2;
    }
